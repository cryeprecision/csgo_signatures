import { Signature as SigRepo } from './kittenpopo_repo'
import { Signature as SigSite } from './kittenpopo_site'

export const mergeSignatures = (repo: SigSite[], site: SigRepo[]): SigSite[] => {
  return []
}
